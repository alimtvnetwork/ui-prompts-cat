# ui-prompts-cat

https://cat-my-v12.ai-memory.app/setup/rules/14



Okay. Uh, we have this image. Put this image into the assets folder in the root of the repo, okay? And also create a prompts folder, zero one prompts folder. Inside this, create the first prompt as, um, pattern search, uh, spec.md file. So that should be zero one pattern search spec inside this prompt folder you should create, okay? That MD file should contain full spec for this UI to implement in the existing system. Okay? So previously, I have explained that we should have one which is the modern UI, another is the standard UI. So as we make the standard UI, we will need to use a switch in every case to switch between the UIs. So this is the unique way system that we'll use with this, uh, this, uh, standard UI. Okay? So the modern UI should remain there. We need to have the same code. We need to have this. Okay. Make sure that this remains, um... Okay. That's-- This is how you need to explain to AI. So your job is to explain it to AI how I, I'm explaining. You have to explain that to AI in detailed manner so that the AI understand every bits and piece of it, so there is nothing missing. And also the image I have given you, you should embed that image as a base64 encoding. Okay? Okay. Okay. Now, coming to the image. The first part of the image, I do see the unit time. So how much time that, uh... We don't know about the unit time, but you can just put it as unit time. We will have more information about this, and we will update you. You can put that to the context. Then you have, like, counts. How many counts of pockets, I think, uh, for the system. So you can just mention this. The AI will, will implement this. They will understand the pocket size and things like that. Okay. Then we have, like, juggle label, like X, Y position, angles, match percentage. So that means these rectangular items that we see in the right-hand side, like search region, pattern region. Um, so the search region is the region where we only search for the item. Okay? And the pattern region is what we're looking for, which type of pattern. So we have, uh, rectangle, circular, many other. So new pattern will come. Okay? You can see the existing settings and see, like, what type... And, and this should come from one place. Okay? So if we change or add a new pattern to the modern design, it would come up here. It's not like it's going to be separate or duplicated. We should try to have DRY, what-- do not repeat yourself code, uh, which should be the much more priority, which you should include in the, uh, spec, uh, when instructing to the AI. Okay? So every part of this thing, like the pattern search, how it is represented with T zero one, you explain that for an AI. You explain the pattern search that we have selected. Uh, you explain the dropdown. It has the reference image, then the raw image. Every one of the button, you explain for the U- AI so that it can understand the zoom in, zoom out feature, forty percent. What is the percentage means? I don't have any idea, but write it like this so that the UI is there. What is the next arrow button is there on top of the header. You can see there is a question mark. You have to explain in very detail. Try to have, like, smaller image of chunks so that the AI can understand on that part. Uh, you have to explain the, uh, reference image section one to hyphen zero, what that means. Uh, we do have no clue what that is, so it needs to be explained later. But you can just put a question mark there for the AI to ask it later. And then inside this, we have the option Search region is the region of interest where we want to search. Pattern search is the pattern that we want to look for, rectangle-based or circular-based or things like that. Uh, and then we can enhance the image option. So the options inside the image enhance will come later. We'll discuss it later. Probably that would be another screen. Okay? And, uh, one to zero zero, I think that is the, uh, part of the reference that we wanted to... Uh, yes. So the reference image, I think the afterwards, once we select the pattern, it gives another, uh, screen or another UI which actually have this, uh, angle range, the change of things that we could do. Okay? Angle range, search, uh, sensitivity, uh, with a slider, accuracy with a slider, uh, with a normal which is in the right side. Okay? Also, the minimum pattern match, which is in the forty percent range, which is what we were seeing at the forty percent. I think that, that's what it is in the forty percent on top of the header black region. Okay? Also explain all this button that we have in the bo- in the below, like the original, uh, origin/point. Explain this very detailed manner, every one of the button. Uh, also the display button, uh, Okay, and Cancel button. This would act like the normal one. Uh, also there is this dot dot button and register image. Keep this button as it is, explaining the color, uh, features, how it would look like. The Run button, Run button settings option. Everything you should explain for the AI, where that is, how that is, uh, what is the portion of it, so that the AI can follow through, understand it, and can implement this standalone with the URL and things like that. Is it clear for you? Do you have any question, concern? I will give you the next image as well according to this, so that you can follow this both image together in the spec and put things, uh, together. Is it clear for you?




The entire image, uh, is actually, um, actually the region selection. Once we have the selection, uh, we can have marked region zero, one, two, three. These are the properties. Each one of the region can have drop-down, have new options. So this should be available in the modern UI as well. So options should be coming from the same place. Remember that. The code needs to be very much dry, um, okay? Um, so based on that, based on that, each one of the region user can drag drop. Remember this, a user can drag it and resize it, okay? So that they can visualize what is happening there. Uh, remember to explain that very effectively so that the AI can follow through. Remember this. And explain every one of the buttons like fixed marked regions, how many ones. So in the standard one, the regions are fixed, but in the modern one we have lots of flexibility. Keep it as it is. Okay? The layer-based concept. Okay? So treat this as similar as the layer base, but in this case, marked region can be assumed... Mask region can be assumed as four layers. So in the standard UI we have, like, four layers only. In the modern UI, we have unlimited layers. We can create layers, we have the draw option, many more things. Okay? Also, you have, like, the image region, use image. I don't know what that section does. You have to explain it later, but for now, you can explain the UI so that the AI can follow through. Okay, the first thing you should do is create a conversation folder, uh, zero two hyphen conversation folder and drop what I just said in here. That should be your first job. And then you start writing that prompt for the AI. Also, make sure that, uh, you put all these images that I've given you in the assets folder, which is zero one zero two, uh, inside a folder, uh, called pattern search. So assets, uh, zero one pattern search. Inside this zero one zero two images, we explain and give the name i-image as a, uh, sequence, okay? Uh, so that it's understandable. Okay, lose nothing, have everything, uh, in one MD file so that I can share with AI. Remember that. Any question, concern, let me know. So th-

This project was built with [Lovable](https://lovable.dev).

## Build with Lovable

Continue developing this project in the [Lovable editor](https://lovable.dev/projects/aeccbb2c-0f61-486c-8e3d-3fce75f74ab0).

- **Ship faster**: describe what you want to build and Lovable handles the code.
- **Stay in sync**: every change made in Lovable is committed straight to this repository.
- **Full ownership**: this code is yours. Push to `main` on GitHub and your changes sync back into Lovable, ready for your next prompt.

## Development

Prefer working locally? You need Node.js and npm — [install with nvm](https://github.com/nvm-sh/nvm#installing-and-updating).

```sh
git clone <this-repository-url>
cd <repository-name>
npm i
npm run dev
```
